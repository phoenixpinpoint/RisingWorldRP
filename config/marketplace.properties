# Marketplace item entries are added automatically when the plugin starts.
# For each item, set enabled=false to hide it or change its price in dollars.
# Internal placeholder items and NPC items are always forced to enabled=false.
